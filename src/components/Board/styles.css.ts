import { style } from '@vanilla-extract/css';

export const boardContainer = style({
  display: 'flex',
  gap: 10,
  backgroundColor: '#1D2125',
  padding: 30,
  height: '100vh',
  overflow: 'hidden',
});
